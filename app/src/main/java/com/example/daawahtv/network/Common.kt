package com.example.daawahtv.network

// The one and only definition for RenderedTitle
data class RenderedTitle(
    val rendered: String
)

// The one and only definition for FeaturedImage
data class FeaturedImage(
    val source_url: String?
)